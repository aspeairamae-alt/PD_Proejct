
  # Convert UI Code to Figma

  This is a code bundle for Convert UI Code to Figma. The original project is available at https://www.figma.com/design/SdjDXl3l2ZI6pObjcsIvFw/Convert-UI-Code-to-Figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  